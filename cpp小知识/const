📌 总结一下 C++ 中 const 的位置区别

为了让你更清晰地分辨，我们可以把  const  在函数中的位置做个简单的归纳：

加在函数最前面（如  const int& func() ）：

修饰返回值。告诉外面：“拿到的结果不许改”。

加在函数参数里（如  void func(const int& a) ）：

修饰传入的参数。告诉函数内部：“传进来的东西不许动”。

加在函数最后面（如  void func() const ）：

修饰成员函数本身（也就是  this  指针）。告诉编译器：“这个函数绝不会修改对象的任何成员变量”。